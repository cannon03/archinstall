# ================================
# Autostart Programs (Minimal)
# ================================

# Start Waybar
exec-once = waybar

# Start hyprpaper (wallpaper daemon)
exec-once = hyprpaper -c $HOME/.config/hypr/hyprpaper.conf
